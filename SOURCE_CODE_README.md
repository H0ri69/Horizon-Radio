# SOURCE CODE PACKAGE FOR MOZILLA REVIEWERS

This archive contains the complete source code for the **AI Radio for YouTube Music** Firefox extension.

## 📋 Quick Start for Reviewers

### Prerequisites
- Node.js v18 or higher
- pnpm v8 or higher

### Automated Build (Recommended)
```bash


# 2. Build extension
node build-firefox.js
```

### Manual Build
```bash
# Install dependencies (uses pnpm-lock.yaml for exact versions)
pnpm install

# Build Firefox extension
pnpm build:firefox
```

**Output**: `dist/firefox/` directory containing the built extension

---

## 📁 Source Code Structure

### Included in This Archive
- ✅ `src/` - All TypeScript/React source code (untranspiled)
- ✅ `manifest.firefox.json` - Firefox manifest (source)
- ✅ `package.json` - Dependency list and build scripts
- ✅ `pnpm-lock.yaml` - Exact dependency versions
- ✅ `vite.config.ts` - Build configuration
- ✅ `tailwind.config.js` - CSS processing config
- ✅ `tsconfig.json` - TypeScript config
- ✅ `build-firefox.js` - Automated build script
- ✅ `BUILD_INSTRUCTIONS_FIREFOX.md` - Detailed build guide
- ✅ `icon.png` - Extension icon
- ✅ `src/assets/sweepers/` - Audio assets (included)

### Excluded from This Archive
- ❌ `node_modules/` - Third-party dependencies (installed via `pnpm install`)
- ❌ `dist/` - Build output (generated by build process)
- ❌ `public/`, `docs/` - Documentation assets (not needed for build)
- ❌ `.git/` - Version control history
- ❌ `.env` - Local environment variables (not needed for build)



---

## 🔧 Build Process Overview

### What Happens During Build
1. **TypeScript Compilation**: `.ts`/`.tsx` → `.js`
2. **React/JSX Transformation**: JSX syntax → JavaScript
3. **CSS Processing**: Tailwind CSS → Standard CSS
4. **Bundling**: Multiple files → Optimized bundles
5. **Asset Copying**: Icons, audio files → `dist/firefox/`
6. **Manifest Processing**: `manifest.firefox.json` → `manifest.json`

### Build Tools Used
- **Vite 6.2**: Bundler and build tool
- **TypeScript 5.8**: Type checking and transpilation
- **@crxjs/vite-plugin**: Chrome extension bundler
- **Tailwind CSS**: CSS framework (compiled at build time)
- **PostCSS**: CSS processing

---

## 🎯 Reproducing the Submitted Extension

### Expected Behavior
Running `pnpm build:firefox` should produce an extension functionally identical to the submitted `.zip` file.

### Why File Hashes May Differ
- Vite includes build timestamps in some files
- Node module resolution may vary by OS

### Verifying Functional Equivalence
The extension's behavior and code logic will be identical. You can compare:
- `manifest.json` structure
- Source code mappings in bundled files
- Extension functionality when loaded in Firefox

---

## 🔐 Security & Privacy

### No API Keys Included
The extension requires a **Google Gemini API Key** to function, but:
- ❌ No API key is included in source or build
- ✅ Users provide their own key via settings UI
- ✅ Key is NOT required to build the extension

### Data Collection
The extension declares `data_collection_permissions: { required: ["none"] }` in the manifest, accurately reflecting that it does not collect user data.

---

## 📞 Support

For build issues, please contact the developer through the Firefox Add-on Developer Hub.

---

**Package Date**: 2026-01-16  
**Extension Version**: 1.0.2  
**Target**: Firefox 140+  
**Build Tool**: Vite 6.2 + TypeScript 5.8
